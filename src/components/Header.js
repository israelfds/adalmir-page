import React from 'react';
import './Header.css'; // Certifique-se de que esse arquivo CSS existe

const Header = () => {
  return (
    <header className="header" style={{ backgroundImage: `url(/images/capa.jpg)` }}>
    </header>
  );
};

export default Header;
